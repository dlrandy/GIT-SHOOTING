如何给之前的commit增加修改或者新添加一个文件？
``` bash
git rebase -i [commit_id]
edit
```
rebase能做什么？
1. 合并多个commit成一个新的commit或者重新排序
2. 撤销之前的commit
3. 改变分支的起点

git rebase -i HEAD~n
展示出的commit order是最早的在上面
既可以合并，移除commit还可以排序哈


