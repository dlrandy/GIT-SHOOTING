git config --global user.email 120@qq.copm

git config --global core.editor vim

git config --global alias.graph 'log --graph --oneline'
git config --global alias.lg "log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --branches"

git配置变量的三种级别
1. repository --local
2. user --global
3. system --system

修改提交人信息
``` bash
git config user.name 'jjda'
git commit --amend --reset-author

```
列出所有当前正在使用的配置
git config --list #从local到system

git config --list --local

编辑配置文件 最重要的是要提前备份

自动修复命令错误
git config --global help.autocorrect 10#1秒修正





