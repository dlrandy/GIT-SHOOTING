git tag v1.0.0
git tag
git tag -a v2.0.0 -m 'sss'

tag是和commit绑定的label

tag不能移动。除非是删除后在新建同名的commit

git tag tag_name
git tag -d tag

tag分为两种一种是简单的只有tag name；还有一种就是带有额外信息的
annotated tag。

annotatedtag是reference 和git object(commit, tree, blob)

git tag -a v1.2.3 commit_id


jj











